{
  "name": "twenty-crm-schema-migration",
  "version": "1.0.0",
  "type": "module",
  "description": "Schema migration script for Twenty CRM Salla integration",
  "main": "migrate-schema.ts",
  "scripts": {
    "migrate": "ts-node migrate-schema.ts",
    "migrate:verify": "ts-node migrate-schema.ts --verify-only"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}

